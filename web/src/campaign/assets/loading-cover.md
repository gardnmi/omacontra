# Loading-screen cover

`loading-cover.png` is the exact cover image supplied by the user on 2026-09-24
(`/tmp/codex-clipboard-dbtT1z.png`). Used without cropping or raster edits.
The loading cartridge/console is lightweight HTML/CSS animation, not a new
bitmap. The image is bundled by Vite so it appears before the game runtime loads.
